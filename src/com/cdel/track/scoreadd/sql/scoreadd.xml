<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="scoreadd">

	<select id="getScoreadd" parameterType="Integer" resultType="Scoreadd">
		SELECT adminID, courseClassID ,courseClassName , courseID
		FROM courseclass WHERE adminID = #{adminID}
	</select>
	
</mapper>